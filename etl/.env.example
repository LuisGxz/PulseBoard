# Copy to .env for local runs. In docker compose these are set via the service environment.
ETL_DATABASE_URL=postgresql://pulseboard:PulseBoard_Dev!2026@localhost:55432/pulseboard
ETL_API_KEY=pulseboard-etl-dev-key
